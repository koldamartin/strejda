# OpenCode Server Configuration

# Server Port (default: 4096)
PORT=4096

# Server Hostname (default: 0.0.0.0 for Docker, 127.0.0.1 for local)
HOSTNAME=0.0.0.0

# Node Environment
NODE_ENV=production

# Optional: Add any additional configuration needed for your OpenCode server
# OPENCODE_API_KEY=your_api_key_here
# OPENCODE_MAX_SESSIONS=100
